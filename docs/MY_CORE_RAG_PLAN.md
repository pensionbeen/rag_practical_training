# 🛠️ Core RAG & 옵시디언 입출력 상태 기반 개발 계획서 (정유빈 전용)

## [AI 행동 지침]
이 파일을 읽은 AI는 아래의 `[STATUS: ...]` 값을 스캔하여 판단하라.
1. `[STATUS: DONE]`: 완료된 작업 (수정 금지)
2. `[STATUS: IN_PROGRESS]`: 현재 작업 중인 코드
3. `[STATUS: TODO]`: 미구현 기능 (우선적으로 제안할 것)

---

## 📌 1. 데이터 파싱 모듈 (일반 PDF 전담)
- [STATUS: DONE] **일반 PDF 문서 로더 (Loader) 구현**
  - *현황*: `pdf_parser.py` 파일로 분리되어 업로드된 PDF로부터 안정적으로 텍스트를 추출하는 독립된 로더 모듈이 완료되었습니다.
- [STATUS: DONE] **문맥 보존 텍스트 분할 (Chunking) 로직**
  - *현황*: `pdf_parser.py` 내에 청크마다 원본 파일명 및 페이지 정보를 접두사로 주입하여 RAG 임베딩 문맥의 유실을 방지하는 구조화된 청커 로직이 적용되었습니다.

## 📌 2. 벡터 DB 제어 및 영구 저장 모듈
- [STATUS: DONE] **FAISS 로컬 저장소 로드 로직 (`load_local`)**
  - *현황*: `main.py`에 통합되어 디스크에 캐싱된 로컬 FAISS 인덱스를 정상적으로 로드합니다.
- [STATUS: DONE] **데이터 임베딩 및 저장 로직 (`save_local`)**
  - *현황*: `main.py`에 통합되어 새로운 옵시디언 노트가 추가되거나 변경되었을 때만 자동으로 로컬 인덱스를 갱신하여 저장합니다.

## 📌 3. 검색 및 파이프라인 모듈
- [STATUS: DONE] **FastAPI 기반 웹 서버 환경 구축 (API Framework Setup)** *(누락 항목 추가)*
  - *현황*: 현재 Streamlit 단일 앱 구조로 되어 있어, 팀 인터페이스를 위한 독립된 API 백엔드 서버(FastAPI 등) 구축이 완료되었습니다.
- [STATUS: DONE] **검색기(Retriever) 및 API 라우터 구현**
  - 프론트엔드의 `POST /api/v1/ask` 요청을 처리하고 응답하는 엔드포인트 작성이 완료되었습니다.

## 📌 4. 옵시디언 마크다운 입출력 모듈 (핵심)
- [STATUS: DONE] **개념 파일 병합 로직 (Merge)**
  - *현황*: `ObsiRAG.py` 내에 구현되어 기존 마크다운 파일이 있는 경우 덮어쓰지 않고 이어붙여(append) 병합을 완료합니다.
- [STATUS: DONE] **복습 노트 자동 기입 로직**
  - *현황*: 규격 API인 `POST /api/v1/obsidian/save` 엔드포인트가 작성 완료되었습니다.
- [STATUS: DONE] **동시성 제어 및 충돌 방지 로직 (Lock)**
  - 프론트엔드에서 여러 요청이 동시에 들어올 경우 파일 내용이 깨지는 것을 막기 위해 파이썬 `asyncio.Lock`을 활용한 파일 접근 순차 제어 로직 구현이 완료되었습니다.

## 📌 5. 관련 학술 논문 검색 및 연동 모듈 (신규)
- [STATUS: DONE] **학술 논문 연동 API (ArXiv 등) 검색 로직 구현**
  - *현황*: 검색하려는 개념명과 어울리는 학술 논문 5개를 API로 찾아와 제목, 링크, 저자 정보를 반환하는 백엔드 API 설계 및 구현.
- [STATUS: DONE] **프론트엔드 논문 정보 카드 표출 및 옵시디언 병합 기능 연동**
  - *현황*: Q&A 또는 개념 생성 시 연관 논문을 함께 보여주고, 원하는 논문 정보를 옵시디언 노트에 기록하는 프론트엔드 인터페이스 구축.